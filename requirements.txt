# Vibe Diplomacy dependencies
# Pinned to the engine version verified to run on Python 3.11.
diplomacy==1.1.2
PyNaCl>=1.5.0
PyYAML>=6.0
# Board pictures. Needs a system libcairo at import time; engine/mapviz.py
# degrades to 'no picture' when that is missing, so it is effectively optional.
cairosvg>=2.7
pytest>=7.0
